[project]
name = "nsa"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11,<3.12"
dependencies = [
  "torch",
  "numpy",
  "scikit-learn",
  "einops",
  "tqdm",
  "pillow",
  "networkx",
  "matplotlib",
  "scipy",
]

[dependency-groups]
dev = [
    "pytest>=8.4.2",
    "pytest-cov>=7.0.0",
]
